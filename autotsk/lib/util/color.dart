import 'package:flutter/material.dart';

// all the colours needed will be stored here
// if there is a need for a new colour, just add it and use it accordingly.
// TO USE: import this file into the main file and use the colours based on the variable it is assigned to here.

// Page Color
const mainLightBgColour = Color.fromRGBO(219, 233, 246, 1);
const mainLightBgColour2 = Color.fromRGBO(255, 255, 255, 1);
const signUpBgColour = Color.fromRGBO(223, 229, 243, 1);
const offWhiteColor = Color.fromRGBO(244, 244, 244, 1);
const homePageBgLightPurpleClr = Color.fromRGBO(116, 100, 227, 1);
const homePageBgDarkPurpleClr = Color.fromRGBO(69, 48, 177, 1);

// Buttons
const buttonlightPurpleClr = Color.fromRGBO(83, 80, 213, 1);
const buttondarkPurpleClr = Color.fromRGBO(66, 44, 168, 1);
const buttonLightBlueClr = Color.fromRGBO(55, 203, 243, 1);
const buttondarkBlueClr = Color.fromRGBO(15, 34, 83, 1);
